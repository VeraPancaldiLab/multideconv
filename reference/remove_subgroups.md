# Remove subgroups that have the same method across different signatures

Remove subgroups that have the same method across different signatures

## Usage

``` r
remove_subgroups(groups)
```

## Arguments

- groups:

  Cell groups of features within cell types.

## Value

List of position of groups which have features of same method.
