# Cell labels

A character vector or factor representing the cell type labels for each
cell.

## Usage

``` r
cell_labels
```

## Format

A vector of length equal to the number of cells and in the same order as
it is on the single cell object.

## Examples

``` r
data(cell_labels)
head(cell_labels)
#> [1] "annotated_ct"
```
