# Sample labels

A character vector or factor representing the sample labels for each
cell.

## Usage

``` r
sample_labels
```

## Format

A vector of length equal to the number of cells and in the same order as
it is on the single cell object.

## Examples

``` r
data(sample_labels)
head(sample_labels)
#> [1] "sample"
```
